pre-commit:
  parallel: true
  commands:
    lint:
      glob: '*.{ts,tsx,js}'
      run: npx eslint {staged_files}
    typecheck:
      glob: '*.{ts,tsx}'
      run: npm run typecheck
    format:
      glob: '*.{ts,tsx,js,json,md,css,scss}'
      run: npx prettier --check {staged_files}
